# Кортеж (tuple) — это тот же самый список, только неизменяемый

y = [1, 3, 5, 6]  # это список (list), он изменяемый
x = (1, 3, 5, 6)  # это кортеж (tuple), он неизменяемый

print(len(x))
print(x[3])
